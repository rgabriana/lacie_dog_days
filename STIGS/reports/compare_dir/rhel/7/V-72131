16c16
< CHECK_CONTENT="Verify the operating system generates audit records when successful/unsuccessful attempts to use the \"truncate\" syscall occur.
---
> CHECK_CONTENT="Verify the operating system generates audit records when successful/unsuccessful attempts to use the "truncate" syscall occur.
18c18
< Check the file system rules in \"/etc/audit/audit.rules\" with the following commands:
---
> Check the file system rules in "/etc/audit/audit.rules" with the following commands:
32c32
< If there are no audit rules defined for the \"truncate\" syscall, this is a finding.
---
> If there are no audit rules defined for the "truncate" syscall, this is a finding.
34c34
< If the output does not produce a rule containing \"-F exit=-EPERM\", this is a finding.
---
> If the output does not produce a rule containing "-F exit=-EPERM", this is a finding.
36c36
< If the output does not produce a rule containing \"-F exit=-EACCES\", this is a finding."
---
> If the output does not produce a rule containing "-F exit=-EACCES", this is a finding."
39c39
< FIX_TEXT="Configure the operating system to generate audit records when successful/unsuccessful attempts to use the \"truncate\" syscall occur.
---
> FIX_TEXT="Configure the operating system to generate audit records when successful/unsuccessful attempts to use the "truncate" syscall occur.
41c41
< Add or update the following rule in \"/etc/audit/rules.d/audit.rules\":
---
> Add or update the following rule in "/etc/audit/rules.d/audit.rules":
