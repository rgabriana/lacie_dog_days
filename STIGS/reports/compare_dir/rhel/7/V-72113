16c16
< CHECK_CONTENT="Verify the operating system generates audit records when successful/unsuccessful attempts to use the \"fsetxattr\" syscall occur.
---
> CHECK_CONTENT="Verify the operating system generates audit records when successful/unsuccessful attempts to use the "fsetxattr" syscall occur.
18c18
< Check the file system rules in \"/etc/audit/audit.rules\" with the following commands:
---
> Check the file system rules in "/etc/audit/audit.rules" with the following commands:
28c28
< If there are no audit rules defined for the \"fsetxattr\" syscall, this is a finding."
---
> If there are no audit rules defined for the "fsetxattr" syscall, this is a finding."
31c31
< FIX_TEXT="Configure the operating system to generate audit records when successful/unsuccessful attempts to use the \"fsetxattr\" syscall occur.
---
> FIX_TEXT="Configure the operating system to generate audit records when successful/unsuccessful attempts to use the "fsetxattr" syscall occur.
33c33
< Add or update the following rule in \"/etc/audit/rules.d/audit.rules\":
---
> Add or update the following rule in "/etc/audit/rules.d/audit.rules":
