9c9
< VULNERABILITY_DISCUSSION="Local initialization files for interactive users are used to configure the user\'s shell environment upon logon. Malicious modification of these files could compromise accounts upon logon."
---
> VULNERABILITY_DISCUSSION="Local initialization files for interactive users are used to configure the user's shell environment upon logon. Malicious modification of these files could compromise accounts upon logon."
12c12
< CHECK_CONTENT="Verify the local initialization files of all local interactive users are group-owned by that user\'s primary Group Identifier (GID).
---
> CHECK_CONTENT="Verify the local initialization files of all local interactive users are group-owned by that user's primary Group Identifier (GID).
18c18
< # cut -d: -f 1,4,6 /etc/passwd | egrep \":[1-4][0-9]{3}"
---
> # cut -d: -f 1,4,6 /etc/passwd | egrep ":[1-4][0-9]{3}"
26c26
< Check the group owner of all local interactive user\'s initialization files with the following command:
---
> Check the group owner of all local interactive user's initialization files with the following command:
33c33
< If all local interactive user\'s initialization files are not group-owned by that user\'s primary GID, this is a finding."
---
> If all local interactive user's initialization files are not group-owned by that user's primary GID, this is a finding."
36c36
< FIX_TEXT="Change the group owner of a local interactive user\'s files to the group found in \"/etc/passwd\" for the user. To change the group owner of a local interactive user\'s home directory, use the following command:
---
> FIX_TEXT="Change the group owner of a local interactive user's files to the group found in \"/etc/passwd\" for the user. To change the group owner of a local interactive user's home directory, use the following command:
