16c16
< CHECK_CONTENT="Verify the operating system generates audit records when successful/unsuccessful attempts to use the \"chown\" syscall occur.
---
> CHECK_CONTENT="Verify the operating system generates audit records when successful/unsuccessful attempts to use the "chown" syscall occur.
18c18
< Check the file system rules in \"/etc/audit/audit.rules\" with the following commands:
---
> Check the file system rules in "/etc/audit/audit.rules" with the following commands:
28c28
< If there are no audit rules defined for the \"chown\" syscall, this is a finding."
---
> If there are no audit rules defined for the "chown" syscall, this is a finding."
31c31
< FIX_TEXT="Add or update the following rule in \"/etc/audit/rules.d/audit.rules\":
---
> FIX_TEXT="Add or update the following rule in "/etc/audit/rules.d/audit.rules":
